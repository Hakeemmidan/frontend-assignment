import styled from 'styled-components';

// Spaces between inner elements
export const SpaceBetween = styled.div`
  display: flex;
  justify-content: space-between;
`;

// Bolds inner text
export const Bold = styled.span`
  font-weight: bold;
`;